
{if count($tags) > 0}
<ul class="tags-list">
    {foreach $tags as $tag}
        <li>
            <label for="tag-{$tag->getId()}">
            <span class="tag" style="background-color: {$tag->color|noescape};" data-tag-color="{$tag->color|noescape}">
                <i class="fa fa-tag fa-fw"></i>{$tag->name}
                <input type="checkbox" id="tag-{$tag->getId()}" name="tags[]" class="tag-checkbox" value="{$tag->getId()}"{if array_key_exists($tag->getId(), $pageTags)} checked{/if}>
            </span>
            </label>
        </li>
    {/foreach}
</ul>
{else}
<p>Nebyli nalezeny žádné tagy.<br> <a href="{plink :Tags:Tag:default}">Přejete si nějaké vytvořit?</a></p>
{/if}